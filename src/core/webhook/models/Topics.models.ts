export declare type TopicsType = {
  service: string;
  name: string;
  createdAt: Date | string;
  action: string;
};

export declare type TopicsList = TopicsType;

export declare type TopicsCreate = TopicsType;
